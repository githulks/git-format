{
  "name": "git-format",
  "version": "0.0.1",
  "description": "Parse and pre-process the git --pretty=format: cli flag so we can parse the response to a JSON object.",
  "main": "index.js",
  "scripts": {
    "test": "mocha --reporter spec --ui bdd test.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/3rd-Eden/git-format"
  },
  "keywords": [
    "git",
    "format",
    "pretty",
    "pre-process",
    "json"
  ],
  "author": "Arnout Kazemier",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/3rd-Eden/git-format/issues"
  },
  "homepage": "https://github.com/3rd-Eden/git-format",
  "devDependencies": {
    "assume": "~1.5.0",
    "mocha": "~5.0.0",
    "pre-commit": "~1.2.0"

  }
}
